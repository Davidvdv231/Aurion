apiVersion: v1
kind: Service
metadata:
  name: aurion-api
  namespace: aurion
  labels:
    app: aurion-api
spec:
  type: ClusterIP
  selector:
    app: aurion-api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
